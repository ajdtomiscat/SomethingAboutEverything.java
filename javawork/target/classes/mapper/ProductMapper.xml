<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.secondhand.mapper.ProductMapper">
    <insert id="insert" parameterType="Product">
        INSERT INTO product (user_id, category_id, title, description, price, original_price, images, status)
        VALUES (#{userId}, #{categoryId}, #{title}, #{description}, #{price}, #{originalPrice}, #{images}, #{status})
    </insert>
    
    <select id="findById" parameterType="Long" resultType="Product">
        SELECT p.*, u.nickname as user_nickname, u.avatar as user_avatar, c.name as category_name
        FROM product p
        LEFT JOIN user u ON p.user_id = u.id
        LEFT JOIN category c ON p.category_id = c.id
        WHERE p.id = #{id}
    </select>
    
    <select id="findByUserId" parameterType="Long" resultType="Product">
        SELECT p.*, c.name as category_name
        FROM product p
        LEFT JOIN category c ON p.category_id = c.id
        WHERE p.user_id = #{userId}
        ORDER BY p.created_at DESC
    </select>
    
    <select id="findByCategoryId" parameterType="Long" resultType="Product">
        SELECT p.*, u.nickname as user_nickname, c.name as category_name
        FROM product p
        LEFT JOIN user u ON p.user_id = u.id
        LEFT JOIN category c ON p.category_id = c.id
        WHERE p.category_id = #{categoryId} AND p.status = 1
        ORDER BY p.created_at DESC
    </select>
    
    <select id="search" parameterType="String" resultType="Product">
        SELECT p.*, u.nickname as user_nickname, c.name as category_name
        FROM product p
        LEFT JOIN user u ON p.user_id = u.id
        LEFT JOIN category c ON p.category_id = c.id
        WHERE (p.title LIKE CONCAT('%', #{keyword}, '%') OR p.description LIKE CONCAT('%', #{keyword}, '%'))
        AND p.status = 1
        ORDER BY p.created_at DESC
    </select>
    
    <select id="findAll" resultType="Product">
        SELECT p.*, u.nickname as user_nickname, c.name as category_name
        FROM product p
        LEFT JOIN user u ON p.user_id = u.id
        LEFT JOIN category c ON p.category_id = c.id
        WHERE p.status = 1
        ORDER BY p.created_at DESC
    </select>
    
    <update id="update" parameterType="Product">
        UPDATE product
        <set>
            <if test="categoryId != null">category_id = #{categoryId},</if>
            <if test="title != null">title = #{title},</if>
            <if test="description != null">description = #{description},</if>
            <if test="price != null">price = #{price},</if>
            <if test="originalPrice != null">original_price = #{originalPrice},</if>
            <if test="images != null">images = #{images},</if>
            <if test="status != null">status = #{status},</if>
            updated_at = CURRENT_TIMESTAMP
        </set>
        WHERE id = #{id}
    </update>
    
    <delete id="delete" parameterType="Long">
        DELETE FROM product WHERE id = #{id}
    </delete>
    
    <update id="incrementViews" parameterType="Long">
        UPDATE product SET views = views + 1 WHERE id = #{id}
    </update>
</mapper>
